import * as products from './products';
import * as ingredients from './ingredients';
import * as cart from './cart';

// collects functions from other services
export const Api = {
  products,
  ingredients,
  cart,
};
